# API Keys (Required to enable respective provider)
ANTHROPIC_API_KEY="your_anthropic_api_key_here"       # Required: Format: sk-ant-api03-...
PERPLEXITY_API_KEY="your_perplexity_api_key_here"     # Optional: Format: pplx-...
OPENAI_API_KEY="your_openai_api_key_here"             # Optional, for OpenAI models. Format: sk-proj-...
GOOGLE_API_KEY="your_google_api_key_here"             # Optional, for Google Gemini models.
MISTRAL_API_KEY="your_mistral_key_here"               # Optional, for Mistral AI models.
XAI_API_KEY="YOUR_XAI_KEY_HERE"                       # Optional, for xAI AI models.
GROQ_API_KEY="YOUR_GROQ_KEY_HERE"                     # Optional, for Groq models.
OPENROUTER_API_KEY="YOUR_OPENROUTER_KEY_HERE"         # Optional, for OpenRouter models.
AZURE_OPENAI_API_KEY="your_azure_key_here"            # Optional, for Azure OpenAI models (requires endpoint in .taskmaster/config.json).
OLLAMA_API_KEY="your_ollama_api_key_here"             # Optional: For remote Ollama servers that require authentication.
GITHUB_API_KEY="your_github_api_key_here"             # Optional: For GitHub import/export features. Format: ghp_... or github_pat_...

# Supabase (server-side)
# Required for server operations that use the service role key.
SUPABASE_URL="https://xyzcompany.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="your_supabase_service_role_key_here"  # Server-only secret: NEVER expose to browser

# Zoho Sign integration (server-side only)
# Notes:
#  - Store client secret and refresh token in server environment only (do NOT commit).
#  - To obtain a refresh token: register an app in Zoho developer console and perform a one-time OAuth consent flow for offline access.
#  - ZOHO_INTEGRATION_ENABLED=false enables mock/dev mode (no external Zoho calls).
ZOHO_CLIENT_ID="your_zoho_client_id_here"
ZOHO_CLIENT_SECRET="your_zoho_client_secret_here"
ZOHO_REFRESH_TOKEN="your_zoho_refresh_token_here"           # long-lived refresh token obtained after OAuth grant
ZOHO_SIGN_TEMPLATE_ID="your_zoho_sign_template_id_here"     # template used to create agreements
ZOHO_REDIRECT_URI="https://your-domain.com/api/zoho/callback" # used during OAuth app registration (if needed)
ZOHO_WEBHOOK_SECRET="optional_webhook_secret_if_configured" # verify webhook payloads if available
ZOHO_INTEGRATION_ENABLED="false"                            # set to "true" in production to enable real Zoho API calls

# Additional server-side secrets (if applicable)
# Add other service keys below as required by your environment

# Google Analytics / GTM
GA4_MEASUREMENT_ID="G-XXXXXXXXXX"   # e.g. G-XXXXXXXXXX
GTM_CONTAINER_ID="GTM-XXXXXXX"      # Optional: Google Tag Manager container ID

# Google Workspace (Gmail API) - server-side email sending using OAuth2
GMAIL_CLIENT_ID="your_gmail_client_id_here"
GMAIL_CLIENT_SECRET="your_gmail_client_secret_here"
GMAIL_REFRESH_TOKEN="your_gmail_refresh_token_here"
GMAIL_SENDER="support@novaqy.com"   # Email address to send from (must be permitted by Workspace)
